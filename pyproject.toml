[project]
name = "samba-central-dashboard"
version = "0.1.9"
description = "Central multi-branch SambaPOS metrics dashboard for ERPNext"
requires-python = ">=3.10"

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"
